extends layout 
block content 
    h1.my-2 #{title} 
    ul.list-group 
        each article, i in articles
            li.list-group-item
                a(href='/article/' + article._id)= article.title
                    unless article.meta.visited
                        span.badge.bg-primary.text-light.mx-2 NEW